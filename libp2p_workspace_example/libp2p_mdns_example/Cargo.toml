[package]
name = "libp2p_mdns_example"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libp2p = { version = "0.52.1", features = ["full"] }
tokio = { version = "1.29.0", features = ["full"] }
