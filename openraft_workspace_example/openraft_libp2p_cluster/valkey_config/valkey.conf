# Local Valkey config for the demo Redis-compatible SQLite cache.
bind 127.0.0.1
protected-mode yes
port 6380
timeout 0
tcp-keepalive 300
databases 16

# Keep demo runs ephemeral; scripts pass --dir for per-run data files.
save ""
appendonly no
