[package]
name = "polars_basic_example"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.45"
polars = { version = "0.54.4", features = [
  "lazy",
  "polars-ops",
  "dtype-datetime",
  "temporal",
  "round_series",
  "strings",
] }
