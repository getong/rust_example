[package]
name = "pin_project_example"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
pin-project = "1.0.10"
request = "0.0.8"
tokio = { version = "1.17.0", features = ["rt-multi-thread", "macros"] }
