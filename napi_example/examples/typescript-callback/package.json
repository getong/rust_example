{
  "name": "napi-example-typescript-callback",
  "version": "0.1.0",
  "private": true,
  "description": "Demonstrates Rust calling back into TypeScript through napi",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "example": "npm run build && npm run start"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
