[package]
name = "wasmer_risk_rule_v2"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

# Build with:
#   cargo build --package wasmer_risk_rule_v2 --release --target wasm32-unknown-unknown
# (the host binary also builds this automatically on `cargo run`)
