[package]
name = "helix_db_example"
version = "0.1.0"
edition = "2024"

[dependencies]
helix-db = "2.0.5"
serde = { version = "1.0", features = ["derive"] }
sonic-rs = "0.5.8"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
