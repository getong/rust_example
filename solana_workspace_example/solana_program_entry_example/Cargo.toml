[package]
name = "solana_program_entry_example"
version = "0.1.0"
edition = "2021"

[dependencies]
borsh = { version = "1.4.0", features = ["derive"] }
solana-program = "1.18.11"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
no-entrypoint = []
