# RPC endpoints (comma or whitespace separated). If unset, RPC_URL is used.
RPC_URLS=https://rpc1.example.com,https://rpc2.example.com

# Fallback single endpoint.
RPC_URL=http://localhost:8899

SEND_WORKERS=4
CONFIRM_WORKERS=4
QUEUE_SIZE=256
RPC_POOL_SIZE=8

SEND_TIMEOUT_MS=5000
CONFIRM_TIMEOUT_MS=30000
CONFIRM_RPC_TIMEOUT_MS=2000
CONFIRM_MAX_RETRIES=20
CONFIRM_INITIAL_DELAY_MS=200
CONFIRM_MAX_DELAY_MS=4000

WORKER_IDLE_TICK_MS=5000
TRACE_WORKER=0

# TX_FILE=./transactions.txt
DEMO_COUNT=4
