[package]
name = "freya_clean_architecture_example"
version = "0.1.0"
edition = "2024"

[dependencies]
freya = { version = "0.4.0", features = ["icons", "radio", "router"] }
freya-router = "0.4.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
freya-testing = "0.4.0"
