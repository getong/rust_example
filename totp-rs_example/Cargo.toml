[package]
name = "totp-rs_example"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
totp-rs = { version = "0.7.4", features = ["qr", "serde_support"] }
